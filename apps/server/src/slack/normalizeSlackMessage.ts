import type { SlackMcpMessage, NormalizedSlackMessage } from "shared/types"; import { stableMessageId } from "../utils/ids.js"; import { nowIso } from "../utils/date.js"; import { rawSlackPath, slackMarkdownPath } from "../markdown/paths.js";
export function normalizeSlackMessage(raw:SlackMcpMessage, dataDir="data"): NormalizedSlackMessage { const conversationId=raw.conversationId ?? raw.channelId ?? "unknown"; const ts=raw.ts ?? raw.timestamp ?? String(Date.now()/1000); const receivedAt=nowIso(); const id=raw.id?.startsWith("slack-message-") ? raw.id : stableMessageId(conversationId, ts); return {id,source:"slack",conversationId,conversationName:raw.conversationName ?? raw.channelName,conversationType:raw.conversationType ?? "unknown",userId:raw.userId,userName:raw.userName,text:raw.text ?? "",ts,threadTs:raw.threadTs,permalink:raw.permalink,rawFilePath:rawSlackPath(dataDir,id,receivedAt),markdownPath:slackMarkdownPath(dataDir,id,receivedAt),receivedAt}; }
