import type { ChatMessage } from "shared/types"; import type { AppDb } from "../db.js"; import { newId } from "../../utils/ids.js"; import { nowIso } from "../../utils/date.js";
export class ChatRepository { constructor(private db:AppDb){} add(slackMessageId:string, role:"user"|"assistant", content:string){const msg={id:newId("chat"),slackMessageId,role,content,createdAt:nowIso()}; this.db.prepare("INSERT INTO chat_messages (id,slack_message_id,role,content,created_at) VALUES (?,?,?,?,?)").run(msg.id,msg.slackMessageId,msg.role,msg.content,msg.createdAt); return msg;} list(slackMessageId:string){return this.db.prepare("SELECT * FROM chat_messages WHERE slack_message_id=? ORDER BY created_at ASC").all(slackMessageId).map((r:any)=>({id:r.id,slackMessageId:r.slack_message_id,role:r.role,content:r.content,createdAt:r.created_at})) as ChatMessage[];} }
