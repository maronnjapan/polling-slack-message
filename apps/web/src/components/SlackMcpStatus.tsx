export function SlackMcpStatus({status}:{status:any}){ return <section className="card"><h3>Slack MCP</h3><p className={status?.connected?"ok":"bad"}>{status?.connected?"接続中":"未接続"}</p><p>server: {status?.serverName ?? "slack"}</p><p>tools: {status?.availableTools ?? 0} / required: {String(status?.hasRequiredTools ?? false)}</p>{status?.error&&<p className="bad">{status.error}</p>}</section>; }
