import { useCallback, useEffect, useState } from "react"; import { api } from "../api/client"; import { SlackMcpStatus } from "../components/SlackMcpStatus"; import { PollingStatus } from "../components/PollingStatus"; import { MessageList } from "../components/MessageList";
export function HomePage({navigate}:{navigate:(p:string)=>void}){ const [mcp,setMcp]=useState<any>(); const [ai,setAi]=useState<any>(); const [poll,setPoll]=useState<any>(); const [open,setOpen]=useState<any[]>([]); const [done,setDone]=useState<any[]>([]); const load=useCallback(()=>{api.mcpStatus().then(setMcp).catch(e=>setMcp({connected:false,error:e.message})); api.aiStatus().then(setAi); api.pollingStatus().then(setPoll); api.messages("open").then(r=>setOpen(r.messages)); api.messages("done").then(r=>setDone(r.messages));},[]); useEffect(()=>{load(); const t=setInterval(load,10000); return()=>clearInterval(t);},[load]); return <><section className="hero"><h1>問い合わせ支援Inbox</h1><p>必要そうなSlack問い合わせだけをAI分析し、返信候補・ToDo・ナレッジをローカルで確認します。</p></section><div className="status"><SlackMcpStatus status={mcp}/><section className="card"><h3>AI Provider</h3><p>{ai?.provider} / {ai?.model}</p><p className={ai?.configured?"ok":"bad"}>{ai?.configured?"設定済み":"未設定"}</p></section><PollingStatus status={poll} onChange={load}/></div><MessageList title="未対応問い合わせ" messages={open} navigate={navigate}/><MessageList title="対応済み問い合わせ" messages={done} navigate={navigate}/></>; }
